apiVersion: v1
kind: Service
metadata:
  name: iperf3-service
  namespace: monitoring
spec:
  selector:
    app: iperf3
  ports:
    - protocol: TCP
      port: 8030   # Service port Prometheus will scrape from
      targetPort: 9093   # Port the container is actually exposing
  type: ClusterIP
