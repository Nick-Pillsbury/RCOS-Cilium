apiVersion: v1
kind: ConfigMap
metadata:
  name: cilium-config
  namespace: kube-system
  labels:
    k8s-app: cilium
data:
  cilium.conf: |
    # Enable Prometheus metrics
    prometheus:
      enabled: true
      port: 9090
