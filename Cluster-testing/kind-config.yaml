kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
networking:
  disableDefaultCNI: true  # Disable default CNI
  podSubnet: "10.244.0.0/16"  # Cilium-compatible Pod subnet
  serviceSubnet: "10.96.0.0/12"  # Service subnet
